#ifndef CFLEX_MACROS_H
#define CFLEX_MACROS_H

// By default, these macros are inert and do nothing. A build tool will later be
// used to parse these annotations and generate reflection data.

#define CF_STRUCT(...)
#define CF_FIELD(...)
#define CF_ENUM(...)

#endif // CFLEX_MACROS_H
